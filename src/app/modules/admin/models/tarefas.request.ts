/*
    Objeto de modelo de dados para a requisição
    de cadastro / edição de tarefas
*/
export interface TarefasRequest {


    //atributos
    nome: string;
    data: string;
    prioridade: string;
}
