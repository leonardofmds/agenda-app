/*
    Objeto de modelo de dados para a resposta
    de cadastro de usuário da API
*/
export interface CadastroUsuariosResponse {

    //atributos
    id: string;
    nome: string;
    email: string;
}